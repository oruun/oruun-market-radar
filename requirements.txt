# Relaxed minimums so pip can pick versions with prebuilt wheels
# for whatever Python version is in use (3.11 / 3.12 / 3.13 / 3.14).
# Pinned to specific versions caused Python 3.14 to need Visual Studio
# to build pandas 2.2.2 from source.
pytrends>=4.9
praw>=7.7
PyYAML>=6.0
pandas>=2.3
numpy>=2.1
requests>=2.32
python-dateutil>=2.9
vaderSentiment>=3.3
anthropic>=0.40
